document.addEventListener('DOMContentLoaded', () => {
    // Initialize AOS with slightly adjusted settings
    AOS.init({
        duration: 1000,
        offset: 0,
        once: true,
        easing: 'ease-out'
    });
});